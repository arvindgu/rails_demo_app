<%= form_for(@transaction) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label  "Current balance in your account is" %> <%= @account.amount %><br>
    
    <br>
    <%= f.label "Transaction Type"%><br>
    <%= f.text_field :txn_type, readonly:true %><br>

    <%= f.label "Transfer to Account Number" %><br>
    <%= f.text_field :to_user_id %><br>

    <%= f.label :amount %><br>
    <%= f.text_field :amount %><br>
     
    <%= f.label :remarks%><br>
    <%= f.text_field :remarks %><br>
  </div>

  <div class="actions">
    <%= f.submit "Transfer", :class =>'submit'%>
  </div>

<% end %>
